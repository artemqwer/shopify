<style>
  .acc-page-wrap {
    font-family: var(--font-montserrat), Montserrat, sans-serif;
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 48px 16px;
  }
  .acc-card {
    width: 100%;
    max-width: 440px;
    background: #fff;
  }
  .acc-card h1 {
    font-size: 28px;
    font-weight: 300;
    letter-spacing: 0.05em;
    color: #222;
    margin-bottom: 32px;
    text-align: center;
  }
  .acc-field { margin-bottom: 20px; }
  .acc-label {
    display: block;
    font-size: 10px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #888;
    margin-bottom: 6px;
  }
  .acc-input {
    width: 100%;
    border: none;
    border-bottom: 1px solid #d1d1d1;
    padding: 10px 0;
    font-size: 14px;
    color: #222;
    background: transparent;
    outline: none;
    transition: border-color 0.2s;
    font-family: inherit;
    box-sizing: border-box;
  }
  .acc-input:focus { border-color: #e61a7a; }
  .acc-btn-submit {
    width: 100%;
    background: #222;
    color: #fff;
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    font-weight: 600;
    padding: 16px;
    border: none;
    cursor: pointer;
    transition: background 0.2s;
    margin-top: 8px;
    font-family: inherit;
  }
  .acc-btn-submit:hover { background: #e61a7a; }
  .acc-links {
    display: flex;
    justify-content: space-between;
    margin-top: 20px;
    font-size: 12px;
    flex-wrap: wrap;
    gap: 8px;
  }
  .acc-links a {
    color: #888;
    text-decoration: none;
    letter-spacing: 0.05em;
    transition: color 0.2s;
  }
  .acc-links a:hover { color: #e61a7a; }
  .acc-error {
    background: #fff0f5;
    border-left: 3px solid #e61a7a;
    padding: 12px 16px;
    font-size: 13px;
    color: #c0134e;
    margin-bottom: 20px;
  }
  .acc-success {
    background: #f0fff4;
    border-left: 3px solid #2d7a2d;
    padding: 12px 16px;
    font-size: 13px;
    color: #2d7a2d;
    margin-bottom: 20px;
  }
</style>
